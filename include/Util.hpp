#pragma once

#include <vector>
#include <string>

namespace Util {
    double getRandomRange(double lower, double upper);

    void time_before();

    double time_after();
}; // namespace Util
